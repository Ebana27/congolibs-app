<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème principal (sans action bar, moderne) -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#26AD39</item>
        <item name="colorPrimaryContainer">#1DDA59</item>
        <item name="colorSecondary">#57F52F</item>
        <item name="android:statusBarColor">#26AD39</item>
    </style>

    <!-- Thème sans ActionBar (pour l'activité principale) -->
    <style name="AppTheme.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- Splash screen (Android 12+) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#26AD39</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

</resources>
